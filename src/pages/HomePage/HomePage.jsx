import css from "../HomePage/HomePage.module.css";

export default function HomePage () {
    return (
        <section className={css.homeText}>
            <h1 > HELLO my PHONEBOOK! </h1>
        </section>
    );
}
